class ActionProcessor {
    fun validate(player: Player, action: Action, amount: Int): Boolean {
        TODO()
    }

    fun execute(hand: Hand, player: Player, action: Action, amount: Int): Boolean {
        TODO()
    }
}
